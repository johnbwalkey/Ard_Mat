function squ = square(x)
%       x           (input)   value to be squared
%       y           (output)  the result of the square
squ = x*x;
end
% in command window define value of x -- like x=4
% then, type square (x)

% ignore erro when run as no input in script